[build]
  builder = "dockerfile"
  dockerfilePath = "Dockerfile"

[deploy]
  preDeployCommand = "node scripts/enable-postgis.js"
  healthcheckPath = "/api/v1/health"
  healthcheckTimeout = 60
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
